import { Head, Link, router } from '@inertiajs/react';
import AppLayout from '@/layouts/app-layout';
import { Button } from '@/components/ui/button';
import type { BreadcrumbItem } from '@/types';

const breadcrumbs: BreadcrumbItem[] = [{ title: 'Dashboard', href: '/dashboard' }, { title: 'Mobile App', href: '/admin/mobile/home' }, { title: 'Promo Banners', href: '/admin/mobile/promos' }];
export default function MobilePromosIndex({ promos }: { promos: Array<Record<string, any>> }) { return <AppLayout breadcrumbs={breadcrumbs}><Head title="Mobile Promo Banners" /><main className="flex flex-1 flex-col gap-6 p-6"><header className="flex items-start justify-between"><div><h1 className="text-2xl font-semibold">Mobile Promo Banners</h1><p className="text-sm text-muted-foreground">Promo ini hanya tampil di aplikasi Mobile Indotix, bukan Website.</p></div><Button asChild><Link href="/admin/mobile/promos/create">Tambah Promo</Link></Button></header><div className="overflow-x-auto rounded-xl border bg-card"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-4">Preview</th><th className="p-4">Nama</th><th className="p-4">Status</th><th className="p-4">Urutan</th><th className="p-4">Aksi</th></tr></thead><tbody>{promos.map(p => <tr className="border-b" key={p.id}><td className="p-4"><img src={p.image_url} alt={p.alt_text ?? p.name} className="h-16 w-36 rounded object-cover" /></td><td className="p-4">{p.name}</td><td className="p-4">{p.is_active ? 'Aktif' : 'Nonaktif'}</td><td className="p-4">{p.sort_order}</td><td className="p-4"><Link className="text-primary" href={`/admin/mobile/promos/${p.id}/edit`}>Edit</Link><button className="ml-4 text-destructive" onClick={() => { if (window.confirm('Hapus promo mobile ini?')) router.delete(`/admin/mobile/promos/${p.id}`); }}>Hapus</button></td></tr>)}</tbody></table></div></main></AppLayout>; }
